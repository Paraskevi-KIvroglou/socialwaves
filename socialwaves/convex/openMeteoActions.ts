/**
 * re-exports from `./forecasts` (marine near-shore probes for land-locked points).
 * kept so `beaches` and `api.openMeteoActions` paths stay stable.
 */
export { refreshMarineHandler, refreshMarineForecast } from "./forecasts";
